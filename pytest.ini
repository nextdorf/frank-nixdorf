[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    --verbose
    --tb=short
    --cov=agent_library
    --cov-report=term-missing
    --cov-report=html:htmlcov

[coverage:run]
omit = 
    tests/*
    venv/*